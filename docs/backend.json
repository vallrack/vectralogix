{
  "entities": {
    "Zone": {
      "title": "Zona Geográfica",
      "description": "Representa una zona delimitada en el mapa por el usuario. Permitir total libertad de lectura/escritura a operadores.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "type": { "type": "string", "enum": ["polygon", "rect", "circle"] },
        "color": { "type": "string" },
        "coordinates": { "type": "array", "description": "Lista de puntos lat/lng que definen la zona." },
        "zoom": { "type": "number" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["name", "type", "color", "coordinates"]
    },
    "Route": {
      "title": "Ruta de Entrega",
      "description": "Una secuencia de puntos optimizada por IA. Libertad de gestión total para operadores.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "stops": { "type": "array" },
        "distance": { "type": "number" },
        "duration": { "type": "number" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["name", "stops"]
    },
    "Driver": {
      "title": "Conductor de Flota",
      "description": "Personal operativo con acceso irrestricto de gestión.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "vehicleType": { "type": "string", "enum": ["car", "van", "truck", "motorcycle"] },
        "isAvailable": { "type": "boolean" },
        "performance": { "type": "number" },
        "email": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["name", "vehicleType", "isAvailable"]
    },
    "Order": {
      "title": "Pedido Logístico",
      "description": "Envío operativo gestionable con libertad total por cualquier operador autenticado.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "customerName": { "type": "string" },
        "address": { "type": "string" },
        "status": { "type": "string", "enum": ["pending", "in-transit", "delivered", "cancelled"] },
        "lat": { "type": "number" },
        "lng": { "type": "number" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["customerName", "address", "status"]
    }
  },
  "auth": {
    "providers": ["google.com", "password"]
  },
  "firestore": {
    "/zones/{zoneId}": {
      "schema": "#/backend/entities/Zone",
      "description": "Zonas de cobertura táctica. Acceso total de lectura/escritura para usuarios autenticados."
    },
    "/routes/{routeId}": {
      "schema": "#/backend/entities/Route",
      "description": "Planeación logística por IA. Acceso total de lectura/escritura para usuarios autenticados."
    },
    "/drivers/{driverId}": {
      "schema": "#/backend/entities/Driver",
      "description": "Gestión de flota. Acceso total de lectura/escritura para usuarios autenticados."
    },
    "/orders/{orderId}": {
      "schema": "#/backend/entities/Order",
      "description": "Pedidos en curso. Acceso total de lectura/escritura para usuarios autenticados."
    }
  }
}