
{
  "entities": {
    "Zone": {
      "title": "Zona Geográfica",
      "description": "Representa una zona delimitada en el mapa por el usuario con su nivel de detalle.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "type": { "type": "string", "enum": ["polygon", "rect", "circle"] },
        "color": { "type": "string" },
        "coordinates": { "type": "array", "description": "Lista de puntos lat/lng que definen la zona." },
        "zoom": { "type": "number" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["name", "type", "color", "coordinates"]
    },
    "Route": {
      "title": "Ruta de Entrega",
      "description": "Una secuencia de puntos trazada para una entrega.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "stops": { "type": "array" },
        "distance": { "type": "number" },
        "duration": { "type": "number" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["name", "stops"]
    },
    "Driver": {
      "title": "Conductor de Flota",
      "description": "Información del operador y su vehículo.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "vehicleType": { "type": "string", "enum": ["car", "van", "truck", "motorcycle"] },
        "isAvailable": { "type": "boolean" },
        "performance": { "type": "number" },
        "email": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["name", "vehicleType", "isAvailable"]
    },
    "Order": {
      "title": "Pedido Logístico",
      "description": "Detalles del envío y destino.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "customerName": { "type": "string" },
        "address": { "type": "string" },
        "status": { "type": "string", "enum": ["pending", "in-transit", "delivered", "cancelled"] },
        "lat": { "type": "number" },
        "lng": { "type": "number" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["customerName", "address", "status"]
    }
  },
  "auth": {
    "providers": ["google.com", "password"]
  },
  "firestore": {
    "/zones/{zoneId}": {
      "schema": "#/backend/entities/Zone",
      "description": "Zonas de cobertura. Permitir lectura/escritura a usuarios autenticados."
    },
    "/routes/{routeId}": {
      "schema": "#/backend/entities/Route",
      "description": "Rutas optimizadas. Permitir lectura/escritura a usuarios autenticados."
    },
    "/drivers/{driverId}": {
      "schema": "#/backend/entities/Driver",
      "description": "Personal de flota. Permitir lectura/escritura a usuarios autenticados."
    },
    "/orders/{orderId}": {
      "schema": "#/backend/entities/Order",
      "description": "Pedidos operativos. Permitir lectura/escritura a usuarios autenticados."
    }
  }
}
